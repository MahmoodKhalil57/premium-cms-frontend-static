# Local development — bun dev live-connects to the deployed instance, no local backend.
# The token comes from https://<your site>/_emdash/api/settings/frontend-token (admins, signed in).
BACKEND_URL=https://<your site>
SITE_URL=https://<your site>
EMDASH_API_TOKEN=ec_pat_...
# EMDASH_INCLUDE_DRAFTS=1   # also render drafts locally
